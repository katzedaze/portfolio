# Database
# Development: Use local PostgreSQL
# Production: Use Neon, Vercel Postgres, Supabase, etc.
DATABASE_URL=postgresql://postgres:mypassword@localhost:5432/portfolio_dev

# Better Auth Secret (Generate with: openssl rand -base64 32)
# IMPORTANT: Use a different secret for production!
BETTER_AUTH_SECRET=your-random-secret-key-at-least-32-characters-long

# Better Auth URL
# Development: http://localhost:3000
# Production: https://your-app.vercel.app
BETTER_AUTH_URL=http://localhost:3000

# Node Environment
NODE_ENV=development

# Vercel Blob Storage (for file uploads in production)
# Development: Files are stored in public/uploads/ directory
# Production: Get this token from: https://vercel.com/dashboard/stores
# Create a new Blob store and copy the token
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxx
